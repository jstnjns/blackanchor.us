<!DOCTYPE html>
<html lang="en" ng-app="ba">
  <head>
    {% include 'head' %}
    {{ content_for_header }}
  </head>

  <body class="shop ng-cloak">

    <div id="wrap">
      {% include 'header' %}
      <div id="content">
        {{ content_for_layout }}
      </div>
      {% include 'footer' %}
    </div>

    {% include 'foot' %}

  </body>
</html>